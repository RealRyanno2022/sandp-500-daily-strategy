[project]
name = "massive-daily-strategy"
version = "0.1.0"
description = "Daily trading signal generator using Massive.io stock data."
authors = [{ name = "Your Name" }]
requires-python = ">=3.11"
dependencies = [
    "pandas",
    "httpx",
    "python-dotenv"
]

[tool.hatch.build.targets.wheel]
packages = ["providers"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
